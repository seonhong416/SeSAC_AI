S3 : Object 저장공간 제공

lambda : 서버리스 컴퓨팅 FaaS 상품 
> 서버를 띄우지 않고 간단한 코드 실행
> 특정 기간 또는 특정 주기로 코드 실행
> 트리거가 실행될때만 코드를 실행
	
	FaaS 란 ?
		함수를 서비스로 제공
		함수 구문을 클라우드에 업로드 해둔다면 
		어느 프로그램에서도 단순히 함수 호출을 통해 
		데이터베이스로부터 입출력이 가능

		프로젝트를 여러개의 함수로 쪼개서 등록


람다 활용하여 서비스 구성하기

S3에서 Lambda에 트리거 제공
Lambda와 S3를 이벤트로 연결

> Temperature에 따른 다른 메세지 출력하는 서비스 구성하기

- 람다함수 만들기
- trigger를 불러올 버킷 생성
- 버킷 속성에서 이벤트 알림 생성
- 람다 대시보드에서 버킷과 람다함수가 연결된 것을 확인
- s3버킷에 파일을 업로드하여 람다함수 모니터링 탭에서 view cloudwatch logs에서 이벤트 발생 확인
(- 버킷에 올라가는 파일은 람다함수의 형식에 맞는 파일이 올라가야함)

